import React from "react";
import ToggleCard from "../ToggleCard";

const UserSignUp = () => {
  return (
    <div>
      <ToggleCard title="Allow new users to sign up" label="User Sign up" />
    </div>
  );
};

export default UserSignUp;
